export type Student = {
  id: string;
  name: string;
  regId: string;
  email: string;
  githubId: string;
  count: number;
  createdAt: string;
  updatedAt: string;
};

export type StudentForm = {
  name: string;
  regId: string;
  email: string;
  githubId: string;
};
